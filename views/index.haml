!!!
%html{:xmlns => "http://www.w3.org/1999/xhtml"}
  %head
    %meta{:content => "text/html; charset=utf-8", "http-equiv" => "content-type"}/
    %title= Blog.title
    %link{:href => "/main.css", :rel => "stylesheet", :type => "text/css"}/
    %link{:href => "/feed", :rel => "alternate", :title => "Primary Feed", :type => "application/atom+xml"}/
    :javascript
      var _gaq = _gaq || [];
      _gaq.push(['_setAccount', 'UA-21486770-1']);
      _gaq.push(['_trackPageview']);

      (function() {
        var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
        ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
        var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
      })();

  %body
    #header
      #logo
        %h1
          %a{:href => "/"}= Blog.title
        %p
          by #{Blog.author}
    #content
      - posts.each do |post|
        .post
          %p.date
            = post[:created_at].strftime("%b")
            %b= post[:created_at].strftime("%d")
          %h2.title
            %a{:href => post.url}= post[:title]
          %p.meta
            %small
              = post.linked_tags
              - if Blog.disqus_shortname
                |
                %a{:href => "#{post.url}#disqus_thread"} comments
          .entry
            = post.summary_html
            - if post.more?
              %a{:href => post.url} Continue reading &raquo;
      - if posts.empty?
        %h2/
        %p Looks like this is a fresh install of Scanty.
        %p
          %a{:href => "/auth"}> Log in
          , then
          = succeed "." do
            %a{:href => "/posts/new"} create a post
      - else
        #older_posts
          %a{:href => "/past"} Archive
      - if admin?
        #new_post
          %a{:href => "/posts/new"}New post

    %a{:href => "https://github.com/chischaschos/scanty"}
      %img{:alt => "Fork me on GitHub", :src => "/images/github-ribbon.png", :style => "position: absolute; top: 0; right: 0; border: 0;"}/

    - if Blog.disqus_shortname
      :javascript
        (function() {
          var links = document.getElementsByTagName('a');
          var query = '?';
          for(var i = 0; i < links.length; i++) {
            if(links[i].href.indexOf('#disqus_thread') >= 0) {
              query += 'url' + i + '=' + encodeURIComponent(links[i].href) + '&';
            }
          }
          document.write('<script type="text/javascript" src="http://disqus.com/forums/#{Blog.disqus_shortname}/get_num_replies.js' + query + '"></' + 'script>');
          })();

    #footer
      %p#legal
        Content by #{Blog.author} |
        Design adapted from
        %a{:href => "http://www.freecsstemplates.org/preview/replenish"} Replenish
